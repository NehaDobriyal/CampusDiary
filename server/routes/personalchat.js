/*
import express from 'express';
import { getPersonalMessages, sendPersonalMessage } from '../controller/personalchat.js';
import { requiresLogin } from '../middleware/auth.js';
const router = express.Router();
router.use(requiresLogin);
router.post('/sendpersonal',sendPersonalMessage);
router.get('/getpersonal/:chatwithId',getPersonalMessages);

export default router;
*/